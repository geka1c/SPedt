
&НаСервереБезКонтекста
Процедура СписокПриПолученииДанныхНаСервере(ИмяЭлемента, Настройки, Строки)
	Для каждого строкаСписка из Строки Цикл
		Если строкаСписка.Ключ.Заказы.Количество()>0 Тогда
			строкаСписка.Значение.Данные.Участник = строкаСписка.Ключ.Заказы[0].Участник;
		КонецЕсли;	
	КонецЦикла;	
КонецПроцедуры
