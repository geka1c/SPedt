
Процедура ПриЗаписи(Отказ)
	Если ПометкаУдаления и ЗначениеЗаполнено(ДокументПоступления) и не ДокументПоступления.ПометкаУдаления Тогда
		об = ДокументПоступления.ПолучитьОбъект();
		об.УстановитьПометкуУдаления(Истина);
		об.Записать()
	КонецЕсли;	
КонецПроцедуры
                                            