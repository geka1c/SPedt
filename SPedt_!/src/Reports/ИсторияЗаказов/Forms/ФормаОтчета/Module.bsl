
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если	Параметры.Свойство("Мегаордер") Тогда
		Если ТипЗнч(Параметры.Мегаордер) = Тип("СправочникСсылка.Посылки") Тогда
			Отчет.Мегаордер.Добавить(Параметры.Мегаордер.ШК);
		Иначе	
			Отчет.Мегаордер.Добавить(Параметры.Мегаордер);
		КонецЕсли;
	КонецЕсли;
КонецПроцедуры
	
