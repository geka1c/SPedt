
&НаСервере
Процедура ЗаполнитьНаСервере()
	СП_ОбновлениеСервер.ЗаполнитьВерсииКонфигураций();
КонецПроцедуры

&НаКлиенте
Процедура Заполнить(Команда)
	ЗаполнитьНаСервере();
	Элементы.Список.Обновить();
КонецПроцедуры

&НаКлиенте
Процедура УстановитьОбновление(Команда)
	
	СП_Обновление.УстановитьОбновление(ЭтотОбъект, Элементы.Список.ТекущиеДанные.ссылка);

КонецПроцедуры
