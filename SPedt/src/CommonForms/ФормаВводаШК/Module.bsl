
&НаКлиенте
Процедура ок(Команда)
	Закрыть(СтрЗаменить(ШК," ",""));
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Параметры.Свойство("Заголовок") Тогда
		Заголовок = Параметры.Заголовок;		
	КонецЕсли;	
	
	Если Параметры.Свойство("ПолеВвода") Тогда
		Элементы.ШК.Заголовок = Параметры.ПолеВвода;		
	КонецЕсли;	
	
	
КонецПроцедуры
